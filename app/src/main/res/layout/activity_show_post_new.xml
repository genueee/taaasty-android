<android.support.design.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:id="@+id/activity_root"
    tools:context=".ui.post.ShowPostActivity2"
    tools:ignore="MergeRootFrame">

    <FrameLayout
        android:id="@+id/container"
        android:layout_width="match_parent"
        android:layout_height="match_parent"/>

    <android.support.v7.widget.Toolbar
        android:id="@+id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="?actionBarSize"
        android:background="@color/semi_transparent_action_bar_light"
        android:theme="@style/ToolbarThemeLight"
        app:title=""
        app:contentInsetLeft="0dp"
        app:contentInsetRight="8dp"
        app:navigationContentDescription="@string/action_back_content_description"
        app:navigationIcon="@drawable/ic_back"
        app:popupTheme="@style/ThemeOverlay.AppCompat.Light"
        tools:title="Маленькая победа..." />

</android.support.design.widget.CoordinatorLayout>